name: 'Lock down repository'

on:
  pull_request:
    types: opened

jobs:
  lockdown:
    runs-on: ubuntu-latest
    steps:
      - uses: dessant/repo-lockdown@v2
        with:
          github-token: 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
          process-only: prs
